<h1><%= @thought.name %></h1>

<p class="thought-indicator"><%= @thought.indicator %>: </p>
<p class="thought-description"><%= @thought.description %></p>

<ul class="step-list">
  <% @steps.each do |step| %>
    <li><%= link_to step.name, thought_step_path(@thought, step) %></li>
  <% end %>
</ol>

<%= link_to "Back to Thoughts", thoughts_path, class: 'back-button' %>